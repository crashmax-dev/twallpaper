{
  "name": "telegram-wallpaper",
  "description": "telegram-wallpaper",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel src/index.html --no-cache --host 127.0.0.1 --port 3000",
    "build": "parcel build src/index.html --no-source-maps --public-url ./",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix"
  },
  "staticFiles": {
    "staticPath": "static"
  },
  "devDependencies": {
    "@crashmax/eslint-config": "^1.1.0",
    "@tweakpane/core": "^1.0.8",
    "parcel": "^2.3.2",
    "parcel-reporter-clean-dist": "^1.0.4",
    "parcel-reporter-static-files-copy": "^1.3.4",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "tweakpane": "^3.0.7"
  }
}
